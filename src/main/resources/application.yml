spring:
  application:
    name: dsVendas
  datasource:
    url: jdbc:postgresql://localhost:5432/dbvendas
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        jdbc:
          lob:
            non-contextual-creation: true